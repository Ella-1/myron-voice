# --- FastAPI Transcriber Configuration ---

# 1. WHISPER_MODEL: Controls the size and speed of the AI model.
#    Options: tiny, base, small, medium (and their .en counterparts).
#    Recommendation: Use 'small' for a good balance of speed and quality.
WHISPER_MODEL=small

# 2. SOURCE_LANGUAGE: Hint to the model about the input language.
#    Leave this blank for automatic language detection (recommended).
#    If you know the language, use a 2-letter code (e.g., sw, fr, es).
SOURCE_LANGUAGE=